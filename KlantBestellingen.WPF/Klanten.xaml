<Window x:Class="KlantBestellingen.WPF.Klanten"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KlantBestellingen.WPF"
        mc:Ignorable="d"
        Title="Klanten" Height="450" Width="800">
    <Grid>
        <!-- 1ST ROW -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <!-- FIRST ROW -->
        <DataGrid x:Name="dgKlanten" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="5" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="True" AlternatingRowBackground="#FFB9BBFF" IsReadOnly="True"
                  PreviewKeyDown="dgKlanten_PreviewKeyDown">
            <DataGrid.Columns>
                <!-- Bind Klant properties to column -->
                <DataGridTextColumn Header="Naam" Binding="{Binding Naam}" MinWidth="200"></DataGridTextColumn>
                <DataGridTextColumn Header="Adres" Binding="{Binding Adres}" MinWidth="200"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <!-- SECOND ROW -->
        <Grid Grid.Row="1" Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,15,0" Background="Black" Foreground="#FFD4D4D4" Padding="5,0,5,0"><Bold>Nieuwe Klant</Bold></TextBlock>
            <TextBlock Text="Naam" Grid.Row="0" Grid.Column="1" Margin="0,0,5,0"/>
            <TextBox x:Name="TbKlantNaam" Grid.Row="0" Grid.Column="2" MinWidth="150" Margin="0,0,10,0" KeyUp="Tb_KeyUp"/>
            <TextBlock Text="Adres" Grid.Row="0" Grid.Column="3" Margin="0,0,5,0"/>
            <TextBox x:Name="TbKlantAdres" Grid.Row="0" Grid.Column="4" MinWidth="300" KeyUp="Tb_KeyUp"/>
        </Grid>
        <Button x:Name="BtnNieuweKlant" Grid.Row="1" Grid.Column="1" Margin="5" Content="Voeg toe" HorizontalAlignment="Right" 
                VerticalAlignment="Center" Width="100" Click="BtnNieuweKlant_Click" IsEnabled="False"/>
    </Grid>
</Window>
